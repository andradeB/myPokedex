import metrics from './metrics';

const button = {
  width: 160,
  height: 60,
  borderRadius: metrics.defaultBorderRadius,
};

const page = {
  padding: metrics.defaultPadding,
};

export {button, page};
